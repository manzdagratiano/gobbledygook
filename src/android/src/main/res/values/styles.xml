<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The theme below is a "NoActionBar" theme to prevent use
         of the native ActionBar, since we would be using the ToolBar
         class instead of the ActionBar class to provide the App bar -->
    <style name="TengenToppaTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_material_dark</item>
    </style>
</resources>
